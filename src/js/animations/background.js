// Grid background is now handled purely via CSS (body::before / body::after)
// using color-mix() with var(--color-primary) so colors auto-cycle with theme.
// This file is kept for the import in main.js.
export function initBackground() {
  // No-op — grid is CSS-only now
}
